const { expect } = require("chai");
const { ethers } = require("hardhat");

describe("Shark Finder Tests", function () {
  it("Should test something more meaningful", async function () {
    expect(1).to.be.(1);
  });
});
